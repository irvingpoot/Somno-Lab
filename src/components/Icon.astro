---

/**
 * @component Icon
 * @description Wrapper posicionado para el menú de usuario de Clerk.
 * * NOTA SOBRE EL NOMBRE:
 * - Aunque se llama "Icon", su función real es renderizar el botón de perfil/avatar (`<UserButton />`).
 * * ESTILO:
 * - Posicionamiento absoluto fijo en la esquina superior derecha del contenedor relativo padre.
 * - Muestra el nombre del usuario junto al avatar (`showName`).
 */

import { UserButton } from "@clerk/astro/components";
---

<div class="absolute top-10 right-10">
    <UserButton showName />
</div>