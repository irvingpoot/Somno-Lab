---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';

import { getCollection } from 'astro:content';

const allPosts = await getCollection('blog');

const gridPosts = allPosts.slice(0, 5); 

const postsIzquierda = gridPosts.slice(0, 2);
const postCentral = gridPosts[2]; 
const postsDerecha = gridPosts.slice(3, 5);

const proximosEventos = [
    {
        dia: "25",
        mes: "ENE",
        titulo: "Masterclass: Sueño y Metabolismo",
        hora: "19:00 hrs",
        lugar: "Zoom Live",
        img: "https://images.unsplash.com/photo-1517048676732-d65bc937f952?q=80&w=800",
        categoria: "Salud Integral"
    },
    {
        dia: "08",
        mes: "FEB",
        titulo: "Workshop: Domina tu CPAP",
        hora: "10:00 AM",
        lugar: "Auditorio SomnoLab",
        img: "https://images.unsplash.com/photo-1576091160399-112ba8d25d1d?q=80&w=800",
        categoria: "Taller Técnico"
    },
    {
        dia: "15",
        mes: "MAR",
        titulo: "Simposio: Futuro del Descanso",
        hora: "09:00 AM",
        lugar: "Hotel Hyatt Mérida",
        img: "https://images.unsplash.com/photo-1544531586-fde5298cdd40?q=80&w=800",
        categoria: "Conferencia"
    }
];

---

<Layout title="Inicio | Somno-Lab">
    <Navbar />
    <section class="relative min-h-screen flex flex-col justify-center items-center overflow-hidden bg-slate-50">
        <div class="absolute top-0 left-1/2 -translate-x-1/2 w-125 h-125 bg-somno/20 rounded-full blur-[100px] pointer-events-none"></div>

        <div class="relative z-10 max-w-5xl mx-auto flex flex-col items-center text-center px-4 animate-fade-in-up">
            <img src="/logoSomno.webp" alt="Logo Somnomid" class="w-24 md:w-30 h-auto mb-8 rounded-2xl shadow-sm opacity-90 hover:opacity-100 transition-opacity bg-slate-50" />
            <h1 class="font-black tracking-tighter text-7xl md:text-9xl mb-6">
                <span class="bg-clip-text text-transparent bg-linear-to-r from-somno-light via-somno to-somno-dark pr-4">
                    SomnoLab
                </span>
            </h1>
            <p class="text-xl md:text-2xl text-slate-800 max-w-2xl font-light leading-relaxed mb-12">
                Ciencia aplicada a tu descanso. <br class="hidden md:block"/>
                <span class="text-slate-500 text-lg">Optimiza tus ciclos de sueño con precisión clínica.</span>
            </p>
            <div class="flex flex-col sm:flex-row gap-5 w-full justify-center">
                <a href="#calculadora" 
                    class="group relative px-10 py-4 bg-somno text-white rounded-full text-lg font-semibold shadow-lg shadow-blue-500/30 hover:shadow-blue-500/50 hover:-translate-y-1 transition-all duration-300 overflow-hidden">
                    <span class="relative z-10 flex items-center justify-center gap-2">
                        <i class="fas fa-calculator text-sm"></i> Calculadora
                    </span>
                    <div class="absolute inset-0 bg-linear-to-r from-transparent via-white/20 to-transparent -translate-x-full group-hover:translate-x-full transition-transform duration-700 ease-in-out"></div>
                </a>
                <a href="/cronograma" 
                    class="group px-10 py-4 bg-white text-slate-700 border border-slate-200 rounded-full text-lg font-semibold hover:border-somno hover:text-somno hover:shadow-lg hover:-translate-y-1 transition-all duration-300">
                    <span class="flex items-center justify-center gap-2">
                        <i class="fas fa-calendar-alt text-sm"></i> Crear Cronograma
                    </span>
                </a>
            </div>
        </div>
        <div class="absolute bottom-12 animate-bounce-slow">
            <a href="#quienes-somos" class="text-slate-300 hover:text-somno transition-colors p-2">
                <i class="fas fa-chevron-down text-2xl"></i>
            </a>
        </div>
    </section>

    <section id="quienes-somos" class="py-24 overflow-hidden">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
                
                <div class="relative">
                    <div class="absolute -top-4 -left-4 w-64 h-64 bg-somno/5 rounded-full blur-3xl"></div>
                    <div class="relative rounded-3xl overflow-hidden shadow-2xl transform hover:scale-[1.02] transition-transform duration-500">
                        <img 
                            src="https://images.unsplash.com/photo-1659989693409-5adc97274bed?q=80&w=687&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D" 
                            alt="Equipo SomnoLab" 
                            class="w-full h-[500px] object-cover"
                            loading="lazy"
                        />
                        <div class="absolute inset-0 bg-linear-to-t from-somno/20 to-transparent"></div>
                    </div>
                    <div class="absolute -bottom-6 -right-6 bg-white p-6 rounded-2xl shadow-xl border border-slate-50 animate-bounce-slow">
                        <p class="text-somno font-black text-4xl">10+</p>
                        <p class="text-slate-500 text-sm font-medium">Años de experiencia<br/>clínica</p>
                    </div>
                </div>

                <div class="space-y-8">
                    <div class="space-y-4">
                        <h2 class="text-somno font-bold tracking-wider uppercase text-sm">Nuestra Filosofía</h2>
                        <h3 class="text-4xl md:text-5xl font-black text-slate-800 tracking-tight leading-tight">
                            Comprometidos con tu <span class="text-somno-light">descanso</span>
                        </h3>
                        <p class="text-lg text-slate-600 leading-relaxed font-light">
                            En <span class="font-semibold text-somno">SomnoLab</span>, fusionamos la tecnología con protocolos médicos para transformar tu salud a través del sueño, diseñando soluciones personalizadas para tu bienestar.
                        </p>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div class="flex gap-4 items-start">
                            <div class="shrink-0 w-12 h-12 bg-slate-50 rounded-xl flex items-center justify-center text-somno border border-slate-100">
                                <i class="fas fa-microscope text-xl"></i>
                            </div>
                            <div>
                                <h4 class="font-bold text-slate-800">Precisión Clínica</h4>
                                <p class="text-sm text-slate-500">Resultados basados en tus reportes médicos.</p>
                            </div>
                        </div>

                        <div class="flex gap-4 items-start">
                            <div class="shrink-0 w-12 h-12 bg-slate-50 rounded-xl flex items-center justify-center text-somno border border-slate-100">
                                <i class="fas fa-user-md text-xl"></i>
                            </div>
                            <div>
                                <h4 class="font-bold text-slate-800">Atención Humana</h4>
                                <p class="text-sm text-slate-500">Acompañamiento médico en cada paso.</p>
                            </div>
                        </div>
                    </div>

                    <div class="pt-4">
                        <a href="/quienes-somos" class="inline-flex items-center gap-2 text-somno font-bold hover:text-somno-dark transition-colors group">
                            Conoce más de nosotros
                            <i class="fas fa-arrow-right text-sm group-hover:translate-x-1 transition-transform"></i>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section id="calculadora" class="relative min-h-screen w-full bg-slate-50 py-24 px-4 overflow-hidden">

        <div class="max-w-7xl mx-auto relative z-10">
            <div class="text-center mb-16 space-y-4">
                <h2 class="text-3xl md:text-5xl font-bold text-slate-800 tracking-tight">
                    Calcule la mejor hora para <span class="text-somno">acostarse</span>
                </h2>
                <p class="text-gray-500 text-lg max-w-2xl mx-auto font-light">
                    Los niños, adolescentes y adultos pueden usar esta calculadora para identificar una hora de acostarse adecuada. 
                    Simplemente seleccione su edad y elija la hora de despertarse para ver los resultados.
                </p>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 items-start">
                <div class="lg:col-span-5 bg-white p-8 rounded-4xl border border-slate-100 shadow-[0_10px_30px_rgba(0,0,0,0.03)] h-fit sticky top-8">
                    <img loading="lazy" src="/logoSomno.webp" alt="Logo SomnoLab" class="w-20 md:w-24 h-auto mb-8 rounded-2xl opacity-90 absolute right-1 top-1 hover:opacity-100 transition-opacity" />
                    <div class="mb-8">
                        <h3 class="text-2xl font-bold text-slate-800 mb-2">Comencemos</h3>
                        <p class="text-somno">Ingresa tus datos para calcular tu horario.</p>
                    </div>
                    
                    <form id="edad-hora" class="flex flex-col gap-6">
                        <div class="space-y-2">
                            <label for="edad" class="text-sm font-semibold text-slate-800 ml-1">Tu Edad</label>
                            <input type="number" id="edad" name="edad" placeholder="Ej. 25"
                                class="w-full p-4 bg-slate-50 border border-slate-200 rounded-xl text-slate-700 font-medium focus:ring-2 focus:ring-somno focus:bg-white transition-all outline-none" />
                        </div>
                        
                        <div class="space-y-2">
                            <label for="hora" class="text-sm font-semibold text-slate-800 ml-1">¿A qué hora quieres despertar?</label>
                            <input type="time" id="hora" name="hora" 
                                class="w-full p-4 bg-slate-50 border border-slate-200 rounded-xl text-slate-700 font-medium focus:ring-2 focus:ring-somno focus:bg-white transition-all outline-none" />
                        </div>

                        <button type="submit" class="mt-4 w-full bg-somno text-white font-bold py-4 px-6 rounded-xl hover:bg-somno-light hover:shadow-lg hover:shadow-blue-500/30 transition-all duration-300 transform active:scale-[0.98]">
                            Calcular Hora Ideal
                        </button>
                    </form>
                </div>

                <div class="lg:col-span-7 w-full">
                    <div class="bg-white rounded-[2.5rem] p-8 md:p-10 border border-slate-100 shadow-sm relative">
                        
                        <div class="flex flex-col md:flex-row gap-8 items-stretch">
                            
                            <div class="shrink-0 bg-black rounded-4xl p-6 flex md:flex-col items-center justify-between md:justify-center gap-6 shadow-2xl shadow-slate-300 min-w-[100px]">
                                <div id="rojo" class="w-16 h-16 rounded-full bg-[#ff0000] border-4 border-black shadow-[0_0_15px_rgba(255,0,0,0.6)]"></div>
                                <div id="amarillo" class="w-16 h-16 rounded-full bg-[#ffe100] border-4 border-black shadow-[0_0_15px_rgba(255,225,0,0.6)]"></div>
                                <div id="verde" class="w-16 h-16 rounded-full bg-[#21c500] border-4 border-black shadow-[0_0_15px_rgba(33,197,0,0.6)]"></div>
                            </div>

                            <div class="grow flex flex-col justify-around gap-4 py-2">
                                <div class="bg-red-50/50 p-4 rounded-2xl border-l-4 border-red-500 flex items-center min-h-18">
                                    <p class="text-red-600 font-semibold text-lg leading-tight" id="mensaje-fuera">Hora fuera del rango ideal</p>
                                </div>
                                <div class="bg-yellow-50/50 p-4 rounded-2xl border-l-4 border-yellow-400 flex items-center min-h-18">
                                    <p class="text-yellow-600 font-semibold text-lg leading-tight" id="mensaje-cerca">Hora cerca del rango ideal</p>
                                </div>
                                <div class="bg-green-50/50 p-4 rounded-2xl border-l-4 border-green-500 flex items-center min-h-18">
                                    <p class="text-green-600 font-semibold text-lg leading-tight" id="mensaje-ideal">Hora ideal</p>
                                </div>
                            </div>
                        </div>

                        <div class="mt-8 pt-6 border-t border-slate-100 text-center">
                            <p id="pie-semaforo" class="text-slate-600 font-medium text-lg leading-relaxed px-4"></p>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </section>

    <section class="py-24 bg-slate-50 relative overflow-hidden">
        <div class="absolute top-0 left-0 w-full h-px bg-linear-to-r from-transparent via-slate-200 to-transparent"></div>

        <div class="max-w-[1400px] mx-auto px-4 sm:px-6">
            
            <div class="flex flex-col md:flex-row justify-between items-end mb-12 gap-6">
                <div>
                    <span class="text-somno font-bold tracking-[0.2em] uppercase text-xs mb-3 block">Eventos SomnoLab</span>
                    <h2 class="text-3xl md:text-5xl font-black text-slate-800 tracking-tight">
                        Proximos <span class="text-transparent bg-clip-text bg-linear-to-r from-somno to-somno-light">Eventos</span>
                    </h2>
                </div>
                <a href="/eventos" class="group flex items-center gap-2 text-slate-500 font-bold hover:text-somno transition-colors">
                    Ver calendario completo 
                    <i class="fas fa-arrow-right transform group-hover:translate-x-1 transition-transform"></i>
                </a>
            </div>

            <div class="grid md:grid-cols-3 gap-8">
                {proximosEventos.map((evento: any) => (
                    <a href="/eventos" class="group bg-white rounded-4xl p-3 border border-slate-100 shadow-sm hover:shadow-2xl transition-all duration-300 hover:-translate-y-2">
                        
                        <div class="relative h-56 rounded-[1.8rem] overflow-hidden mb-5">
                            <img src={evento.img} alt={evento.titulo} class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110" />
                            
                            <div class="absolute top-4 right-4 bg-white/95 backdrop-blur-md rounded-2xl p-2.5 text-center shadow-lg min-w-[65px]">
                                <span class="block text-xl font-black text-slate-800 leading-none">{evento.dia}</span>
                                <span class="block text-[10px] font-bold text-somno uppercase">{evento.mes}</span>
                            </div>

                            <div class="absolute bottom-4 left-4">
                                <span class="bg-slate-900/70 backdrop-blur-md text-white text-[10px] font-bold px-3 py-1.5 rounded-full uppercase tracking-wider border border-white/10">
                                    {evento.categoria}
                                </span>
                            </div>
                        </div>

                        <div class="px-3 pb-3">
                            <h3 class="text-xl font-bold text-slate-800 mb-3 leading-tight group-hover:text-somno transition-colors line-clamp-2">
                                {evento.titulo}
                            </h3>
                            
                            <div class="flex flex-col gap-2 text-sm text-slate-500 border-t border-slate-50 pt-3 mt-auto">
                                <div class="flex items-center gap-3">
                                    <span class="w-6 h-6 rounded-full bg-somno/10 flex items-center justify-center text-somno text-xs">
                                        <i class="far fa-clock"></i>
                                    </span>
                                    {evento.hora}
                                </div>
                                <div class="flex items-center gap-3">
                                    <span class="w-6 h-6 rounded-full bg-somno/10 flex items-center justify-center text-somno text-xs">
                                        <i class="fas fa-map-marker-alt"></i>
                                    </span>
                                    {evento.lugar}
                                </div>
                            </div>
                        </div>
                    </a>
                ))}
            </div>
            
            <div class="mt-8 text-center md:hidden">
                <a href="/eventos" class="inline-block w-full px-8 py-4 bg-slate-100 text-slate-700 font-bold rounded-xl hover:bg-slate-200 transition-colors">
                    Ver todos los eventos
                </a>
            </div>

        </div>
    </section>

    <section id="somnoblog" class="py-24">
        <div class="max-w-[1400px] mx-auto px-4 sm:px-6">
            
            <div class="text-center mb-16">
                <h2 class="text-4xl md:text-6xl font-bold text-slate-800 tracking-[0.2em] uppercase mb-4">
                    Somno<span class="text-transparent bg-clip-text bg-linear-to-r from-somno to-somno-light">Blog</span>
                </h2>
                <p class="text-slate-500 max-w-2xl mx-auto text-lg">
                    Explora nuestra colección de artículos curados por especialistas.
                </p>
            </div>

            <div class="flex flex-col lg:flex-row gap-4 h-auto lg:h-[750px] w-full transition-all duration-500">
                
                <div class="flex flex-col gap-4 flex-1 lg:hover:grow-[1.5] transition-all duration-700 ease-out">
                    {postsIzquierda.map((post) => (
                        <a href={`/blog/${post.data.id}`} class="group relative flex-1 hover:grow-2 transition-all duration-500 rounded-4xl overflow-hidden border border-slate-100 shadow-sm hover:shadow-2xl">
                            <img loading="lazy" src={post.data.img} alt={post.data.titulo} class="absolute inset-0 w-full h-full object-cover transition-transform duration-700 group-hover:scale-110" />
                            <div class="absolute inset-0 bg-linear-to-t from-slate-900/90 via-slate-900/20 to-transparent opacity-80 group-hover:opacity-90 transition-opacity"></div>
                            
                            <div class="absolute bottom-0 left-0 p-8 w-full translate-y-4 group-hover:translate-y-0 transition-transform duration-500">
                                <span class="inline-block px-3 py-1 bg-somno backdrop-blur-md text-white text-[10px] font-bold uppercase tracking-wider rounded-full mb-3">
                                    {post.data.categoria}
                                </span>
                                <h3 class="text-2xl font-bold text-white mb-2 leading-tight">
                                    {post.data.titulo}
                                </h3>
                                <p class="text-slate-300 text-sm opacity-0 h-0 group-hover:h-auto group-hover:opacity-100 transition-all duration-500 delay-100 line-clamp-2">
                                    {post.data.resumen}
                                </p>
                            </div>
                        </a>
                    ))}
                </div>

                {postCentral && (
                    <div class="flex flex-col flex-[1.5] lg:hover:grow-[2.5] transition-all duration-700 ease-out gap-4">
                        <a href={`/blog/${postCentral.data.id}`} class="group relative flex-1 rounded-4xl overflow-hidden border border-slate-100 shadow-lg hover:shadow-2xl transition-all duration-500">
                            <img loading="lazy" src={postCentral.data.img} alt={postCentral.data.titulo} class="absolute inset-0 w-full h-full object-cover transition-transform duration-1000 group-hover:scale-105" />
                            <div class="absolute inset-0 bg-linear-to-b from-transparent via-transparent to-slate-900/90"></div>
                            
                            {postCentral.data.destacado && (
                                <div class="absolute top-8 left-8">
                                    <span class="bg-somno text-white px-4 py-2 rounded-xl text-xs font-bold uppercase tracking-widest shadow-lg shadow-somno/30">Destacado</span>
                                </div>
                            )}

                            <div class="absolute bottom-0 left-0 p-10 md:p-12 w-full">
                                <h3 class="text-4xl md:text-5xl font-black text-white mb-4 leading-none transition-colors">
                                    {postCentral.data.titulo}
                                </h3>
                                <p class="text-lg text-slate-200 mb-6 max-w-md opacity-90 line-clamp-3">
                                    {postCentral.data.resumen}
                                </p>
                                <span class="inline-flex items-center gap-3 bg-white text-slate-900 px-6 py-3 rounded-full font-bold text-sm hover:bg-somno hover:text-white transition-all group-hover:scale-105">
                                    Leer artículo completo <i class="fas fa-arrow-right"></i>
                                </span>
                            </div>
                        </a>
                    </div>
                )}

                <div class="flex flex-col gap-4 flex-1 lg:hover:grow-[1.5] transition-all duration-700 ease-out">
                    {postsDerecha.map((post) => (
                        <a href={`/blog/${post.data.id}`} class="group relative flex-1 hover:grow-2 transition-all duration-500 rounded-4xl overflow-hidden border border-slate-100 shadow-sm hover:shadow-2xl">
                            <img loading="lazy" src={post.data.img} alt={post.data.titulo} class="absolute inset-0 w-full h-full object-cover transition-transform duration-700 group-hover:scale-110" />
                            <div class="absolute inset-0 bg-linear-to-t from-slate-900/90 via-slate-900/20 to-transparent opacity-80 group-hover:opacity-90 transition-opacity"></div>
                            
                            <div class="absolute bottom-0 left-0 p-8 w-full translate-y-4 group-hover:translate-y-0 transition-transform duration-500">
                                <span class="inline-block px-3 py-1 bg-somno text-white text-[10px] font-bold uppercase tracking-wider rounded-full mb-3">
                                    {post.data.categoria}
                                </span>
                                <h3 class="text-2xl font-bold text-white mb-2 leading-tight">
                                    {post.data.titulo}
                                </h3>
                                <p class="text-slate-300 text-sm opacity-0 h-0 group-hover:h-auto group-hover:opacity-100 transition-all duration-500 delay-100 line-clamp-2">
                                    {post.data.resumen}
                                </p>
                            </div>
                        </a>
                    ))}
                </div>

            </div>

            <div class="mt-12 text-center">
                <a href="/blog" class="inline-block w-full md:w-auto px-8 py-4 bg-white text-slate-700 border border-slate-800 rounded-full text-lg font-semibold hover:border-somno hover:text-somno hover:shadow-lg hover:-translate-y-1 transition-all duration-300">
                    Ver todos los artículos
                </a>
            </div>
        </div>
    </section>

    <div id="modal-info" class="hidden fixed inset-0 z-50 items-center justify-center bg-gray-900/60 backdrop-blur-sm p-4 transition-all duration-300">
        <div class="bg-white rounded-3xl shadow-2xl w-full max-w-sm overflow-hidden animate-fade-in-up border border-white/20">
            
            <div class="p-8 text-center">
                <div class="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-orange-50 mb-6">
                    <i class="fas fa-exclamation text-2xl text-orange-400"></i>
                </div>
                
                <h3 class="text-xl font-bold text-gray-900 mb-2">Faltan datos</h3>
                <p class="text-gray-500 text-sm leading-relaxed">
                    Por favor, ingresa tu edad y la hora a la que deseas despertar para calcular tu ciclo.
                </p>
            </div>

            <div class="bg-gray-50 px-6 py-4">
                <button id="close-modal-btn" class="w-full bg-somno text-white font-semibold py-3 px-4 rounded-xl hover:bg-blue-600 transition-colors shadow-lg shadow-blue-500/30">
                    Entendido
                </button>
            </div>
        </div>
    </div>
    
</Layout>

<script src="../scripts/scriptMain.js"></script>

<style>
    /* Estilos base para el semáforo */
    #rojo { background-color: #ff0000; }
    #amarillo { background-color: #ffe100; }
    #verde { background-color: #21c500; }

    
    @keyframes fadeInUp { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
    .animate-fade-in-up { animation: fadeInUp 0.8s cubic-bezier(0.2, 0.8, 0.2, 1) forwards; }
    @keyframes bounceSlow { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(10px); } }
    .animate-bounce-slow { animation: bounceSlow 2s infinite ease-in-out; }
</style>